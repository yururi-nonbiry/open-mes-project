# users/urls.py

app_name = "users"

urlpatterns = [
    # このファイルは、APIベースではないDjangoテンプレートビュー用のURLを定義するために使用されていました。
    # プロジェクトが完全にAPI駆動になったため、現在ここにはURLは定義されていません。
    # 将来的にテンプレートベースのビューが必要になった場合は、ここに追加できます。
]
